apiVersion: v1
kind: PersistentVolume

metadata:
  name: mysql-pv

spec:
  storageClassName: my-storage
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  hostPath: #hostpath를 쓸 때는 ReadWriteOnce를 입력해야한다.
    path: "/mnt/data"